#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../password-generator/bin/password-generator" "$@"
  ret=$?
else 
  node  "$basedir/../password-generator/bin/password-generator" "$@"
  ret=$?
fi
exit $ret
